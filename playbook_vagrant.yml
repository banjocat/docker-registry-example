---

- hosts: all
  gather_facts: no
  become: yes
  roles:
      - python
      - role: docker
        apt_release: main

      - role: docker_registery
        ubuntu_version: ubuntu-xenial
        certs_path: "/tmp/"
        domain: everythingisawesome.com
