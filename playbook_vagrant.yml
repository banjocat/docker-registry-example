---

- hosts: all
  gather_facts: no
  become: yes
  roles:
      - python
      - role: docker
        ubuntu_version: ubuntu-xenial
        apt_release: main

      - role: docker_registery
        certs_path: "/tmp/"
        domain: everythingisawesome.com
